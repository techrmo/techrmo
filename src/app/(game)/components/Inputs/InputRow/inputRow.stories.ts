import InputRow from '.';
import type { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof InputRow> = {
  title: 'Components/InputRow',
  component: InputRow,
  tags: ['autodocs'],
};

export default meta;
export const Default: StoryObj = {};
